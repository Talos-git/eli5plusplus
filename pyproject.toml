[project]
name = "eli5plusplus"
version = "0.1.0"
description = "A Streamlit app that explains topics at adjustable complexity levels."
readme = "README.md"
requires-python = ">=3.10,<3.14"
dependencies = [
    "google-cloud-secret-manager",
    "google-generativeai==0.8.5",
    "python-dotenv",
    "streamlit==1.45.1",
]

[tool.uv]
package = false
